O container de docker está à espera de uma network bridge chamada "GoManager"
Para a criar:
	$ docker network create GoManager

Para correr o docker:
	abrir "RUN_DOCKER_GO_MANAGER.cmd"
	$ cd <pasta do projecto respetivo>
	ver ReadMe incluido em cada um para mais informação